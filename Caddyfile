{
	email {$ACME_EMAIL:admin@localhost}
}

{$DOMAIN:localhost} {
	# Для localhost — внутренний сертификат (без Let's Encrypt)
	tls internal
	encode gzip

	handle /api/* {
		reverse_proxy api:8000
	}

	handle /uploads/* {
		reverse_proxy api:8000
	}

	handle {
		reverse_proxy web:3000
	}
}
